# Examples

`aircraft.sample.json` is a small synthetic readsb/dump1090-style snapshot
created for documentation and local inspection. It is not live aircraft data,
does not represent a real flight, and is not consumed automatically by the
Mystic door.

Real deployments should point the door at their own local
`/home/mystic/doors/data/aircraft.json` file.
